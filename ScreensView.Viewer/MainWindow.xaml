<Window x:Class="ScreensView.Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ScreensView.Viewer.ViewModels"
        Title="ScreensView" Height="700" Width="1100"
        WindowState="Maximized"
        Icon="/screensview.ico"
        Closing="Window_Closing">

    <Window.Resources>
        <Style x:Key="StatusIndicator" TargetType="Ellipse">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Height" Value="10"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Toolbar -->
        <ToolBar DockPanel.Dock="Top" Height="40">
            <Button Content="Управление компьютерами" Click="ManageComputers_Click" Margin="4,0"/>
            <Separator/>
            <TextBlock Text="Интервал (сек):" VerticalAlignment="Center" Margin="4,0"/>
            <Slider x:Name="IntervalSlider" Minimum="1" Maximum="60" Width="120" VerticalAlignment="Center"
                    Value="{Binding RefreshInterval}" Margin="4,0" TickFrequency="1" IsSnapToTickEnabled="True"/>
            <TextBlock Text="{Binding RefreshInterval, StringFormat='{}{0} с'}" VerticalAlignment="Center" Width="35"/>
            <Separator/>
            <ToggleButton Command="{Binding TogglePollingCommand}" Margin="4,0" Width="90"
                          IsChecked="{Binding IsPolling, Mode=OneWay}">
                <ToggleButton.Style>
                    <Style TargetType="ToggleButton">
                        <Setter Property="Content" Value="Запустить"/>
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Content" Value="Остановить"/>
                                <Setter Property="Background" Value="#FF4444"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>
            <Separator/>
            <Button Content="Обновить агентов" Click="UpdateAllAgents_Click" Margin="4,0"/>
            <Separator/>
            <CheckBox Content="Автозапуск"
                      IsChecked="{Binding IsAutostartEnabled}"
                      VerticalAlignment="Center"
                      Margin="8,0,4,0"/>
            <Separator/>
            <Button Content="О программе" Click="About_Click" Margin="4,0"/>
        </ToolBar>

        <!-- Screenshot Grid -->
        <ScrollViewer x:Name="_screensScrollViewer"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      SizeChanged="ScrollViewer_SizeChanged"
                      ScrollChanged="ScrollViewer_ScrollChanged">
            <ItemsControl ItemsSource="{Binding Computers}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Loaded="WrapPanel_Loaded"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ComputerViewModel}">
                        <Border Margin="6" BorderBrush="#CCCCCC" BorderThickness="1"
                                Background="#1E1E1E" CornerRadius="4"
                                MouseLeftButtonDown="Card_MouseLeftButtonDown">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="28"/>
                                </Grid.RowDefinitions>

                                <!-- Screenshot -->
                                <Image Grid.Row="0" Source="{Binding Screenshot}" Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="LowQuality"/>

                                <!-- Placeholder when no screenshot -->
                                <TextBlock Grid.Row="0" Text="Нет изображения" Foreground="#666"
                                           HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Screenshot, Converter={StaticResource NullToBool}}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <!-- Lock overlay -->
                                <Grid Grid.Row="0">
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Locked">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    <Rectangle Fill="#80000000"/>
                                    <TextBlock Text="🔒" FontSize="32"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               Foreground="White"/>
                                </Grid>

                                <!-- Bottom bar -->
                                <Border Grid.Row="1" Background="#2D2D2D">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Status indicator -->
                                        <Ellipse Grid.Column="0">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse" BasedOn="{StaticResource StatusIndicator}">
                                                    <Setter Property="Fill" Value="Gray"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Online">
                                                            <Setter Property="Fill" Value="#44CC44"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Offline">
                                                            <Setter Property="Fill" Value="#CC4444"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Error">
                                                            <Setter Property="Fill" Value="#CCAA00"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Locked">
                                                            <Setter Property="Fill" Value="#CCAA44"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>

                                        <!-- Computer name -->
                                        <TextBlock Grid.Column="1" Text="{Binding Name}" Foreground="White"
                                                   VerticalAlignment="Center" Margin="6,0" FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis" ToolTip="{Binding StatusMessage}"/>

                                        <!-- Last updated time -->
                                        <TextBlock Grid.Column="2" Foreground="#888" FontSize="10"
                                                   VerticalAlignment="Center" Margin="0,0,6,0"
                                                   Text="{Binding LastUpdated, StringFormat='HH:mm:ss'}"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Window>
